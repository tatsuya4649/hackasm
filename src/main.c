
#include <sym.h>
#include <parser.h>
#include <stdio.h>

int main(int argc,const char *argv[])
{
	const char *file;
	if ( !argv[1] || argc<=1 )
		printf("error : not found to .asm file\n");
	/* get file name */
	file = argv[1];
}
